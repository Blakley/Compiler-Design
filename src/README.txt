


Build tree structure:

Extend program to have each nonterminal function should generate a node.
To define the node: note that it needs a label, could be string, and should be the same as the function name generating it
should have any tokens processed in the function, and finally will have children for each nonterminal in a production.
